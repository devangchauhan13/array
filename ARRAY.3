#include<stdio.h>
#include<conio.h>

main()
{
	int a[1000],i,j,r,n,temp;
	clrscr();
	printf("enter array size=");
	scanf("%d",&n);

	for(i=0;i<n;i++)
	{
		printf("a[%d]=",i);
		scanf("%d",&a[i]);
	}
	printf("\nNumber of right rotate an arrey=");
	scanf("%d",&r);
	for(i;i<r;i++)
	{
		temp=a[n-1];
		for(j=n-1;j>0;j--)
		{
			a[j]=a[j-1];
		}
	}
	printf("\nArray elements after right rotate=");
	for(i=0;i<n;i++)
	{
		printf("%d,",a[i]);
	}
	getch();
}